apiVersion : v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
    - name : nginx 
      image: "kotara2905/websaite1:{{image_tag}}"
      ports:
        - containerPort: 80
        - containerPort: 443
  imagePullSecrets:
    - name: regcred
  