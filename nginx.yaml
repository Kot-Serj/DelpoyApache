apiVersion : v1
kind: Pod
metadata:
  name: hello
spec:
  containers:
    - name : container-hello
      image: kotara2905/websaite1:${commitId}
      ports:
        - containerPort: 80
      - containerPort: 443
imagePullSecrets:
  - name: regcred
  