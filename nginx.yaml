apiVersion : v1
kind: Deployment
metadata: 
  name: nginx
spec: 
  containers: 
    - name : nginx-php
      image: kotara2905/websaite1:${commitId}
      ports:
        - containerPort: 80
        - containerPort: 443
  imagePullSecrets:
    - name: regcred
  