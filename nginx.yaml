apiVersion : v1
kind: Deployment
metadata: 
  name: ${namespace}
  containers: 
    - name : {{namespace}}
      image: kotara2905/websaite1:${commitId}
      ports:
        - containerPort: 80
        - containerPort: 443
  imagePullSecrets:
    - name: regcred
  